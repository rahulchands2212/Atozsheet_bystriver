#include<bits/stdc++.h>
using namespace std;

int main(){
float ans = (float)9/2;
int gap = round(ans);
cout<<gap;
ans = (float)gap / 2;
gap = round(ans);
cout<<gap;
ans = (float)gap / 2;
gap = round(ans);
cout<<gap;
ans = (float)gap / 2;
gap = round(ans);
cout<<gap;
ans = (float)gap / 2;
gap = round(ans);
cout<<gap;
ans = (float)gap / 2;
gap = round(ans);
cout<<gap;
return 0;
}